@if (loading)
{
    <MudProgressCircular Color="Color.Default" Indeterminate="true" />
}
else
{

    <MudItem xs="12" md="6">
        <MudSelect T="string" Label=@L["Select Category"] MultiSelection="true" @bind-SelectedValues="Cats">
            @foreach (var cat in categories)
            {
                <MudSelectItem T="string" Value="@cat.Name" />
            }
        </MudSelect>
        <MudSelect T="string" Label=@L["Select Questions"] MultiSelection="true" @bind-SelectedValues="Quests">
            @foreach (var q in questions1)
            {
                <MudSelectItem T="string" Value="@q.Name" />
            }
        </MudSelect>
        <MudText Typo="Typo.body2">@L["Selected Categories"]:  @(string.Join(", ", Cats.Select(x=>$"\"{x}\""))) </MudText>
    </MudItem>
    <MudTable Items="@(GetQuestions())" SortLabel=@L["Sort By"] Dense="@dense" Hover="@hover" Bordered="@bordered" Striped="@striped" Breakpoint="Breakpoint.Sm" Filter="new Func<Question,bool>(FilterFunc1)" @bind-SelectedItem="selectedItem1">
        <ColGroup>
            <col />
            <col style="width: 60%;" />
            <col />
        </ColGroup>
        <ToolBarContent>
            <MudText Typo="Typo.h6">@L["Questions Table"]</MudText>
            <MudSpacer />
            <MudTextField @bind-Value="searchString1" Placeholder=@L["Search"] Adornment="Adornment.Start" AdornmentIcon="@Icons.Material.Filled.Search" IconSize="Size.Medium" Class="mt-0"></MudTextField>
        </ToolBarContent>
        <HeaderContent>
            <MudTh><MudTableSortLabel SortBy="new Func<Question, object>(x=>x.Category.Name)">@L["Category"]</MudTableSortLabel></MudTh>
            <MudTh><MudTableSortLabel SortBy="new Func<Question, object>(x=>x.Name)">@L["Question"]</MudTableSortLabel></MudTh>
            <MudTh>@L["Actions"]</MudTh>
        </HeaderContent>
        <RowTemplate>
            <MudTd DataLabel="Category">@context.Category.Name</MudTd>
            <MudTd DataLabel="Question">@context.Name</MudTd>
            <MudTd DataLabel="Actions">
                <MudButton OnClick="@(()=>QuestionDisplay(context))">@L["Display"]</MudButton>
                <MudButton OnClick="@(()=>QuestionDialog(context))">@L["Edit"]</MudButton>
            </MudTd>
        </RowTemplate>
        <PagerContent>
            <MudTablePager HidePagination="true" HorizontalAlignment="HorizontalAlignment.Right" Class="pl-2 pr-2"
                           PageSizeOptions="new int[] { 5, 10, 25, 50, 100 }" RowsPerPageString=@L["Rows per page"]
                           InfoFormat="{first_item}-{last_item} / {all_items}" />
        </PagerContent>
    </MudTable>

    <MudItem class="d-flex flex-wrap mt-4">
        <MudSwitch @bind-Checked="@hover" Color="Color.Primary">@L["Hover"]</MudSwitch>
        <MudSwitch @bind-Checked="@dense" Color="Color.Secondary">@L["Dense"]</MudSwitch>
        <MudSwitch @bind-Checked="@striped" Color="Color.Tertiary">@L["Striped"]</MudSwitch>
        <MudSwitch @bind-Checked="@bordered" Color="Color.Warning">@L["Bordered"]</MudSwitch>
        <MudSpacer />
        <MudItem style="min-width:200px;">
            <MudText Inline="true" Class="align-self-center">@L["Selected1"]: @selectedItem1?.Name</MudText>
        </MudItem>
    </MudItem>
}
<style>
    .mud-tabs-panels {
        min-height: 200px !important;
    }
</style>