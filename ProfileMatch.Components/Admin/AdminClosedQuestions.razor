@if (loading)
{
    <MudProgressCircular Color="Color.Default" Indeterminate="true" />
}
else
{
    <MudItem xs="12" md="6">
        <MudSelect T="string" Label=@L["Select Category"] MultiSelection="true" @bind-SelectedValues="Cats">
            @foreach (var cat in categories)
            {
                <MudSelectItem T="string" Value="@cat.Name" />
            }
        </MudSelect>
        <MudText Typo="Typo.body2">@L["Selected Categories"]:  @(string.Join(", ", Cats.Select(x=>$"\"{x}\""))) </MudText>
    </MudItem>
    <MudTable Items="@(GetQuestions())" SortLabel=@L["Sort By"] Dense="true" Hover="true" Bordered="false" Striped="false" Breakpoint="Breakpoint.Sm" Filter="new Func<ClosedQuestion,bool>(FilterFunc1)" @bind-SelectedItem="selectedItem1">
        <ColGroup>
            <col />
            <col style="width: 60%;" />
            <col />
        </ColGroup>
        <ToolBarContent>
            <MudText Typo="Typo.h6">@L["Closed ClosedQuestions"]</MudText>
            <MudSpacer />
            <MudTextField @bind-Value="searchString1" Placeholder=@L["Search"] Adornment="Adornment.Start" AdornmentIcon="@Icons.Material.Filled.Search" IconSize="Size.Medium" Class="mt-0"></MudTextField>
        </ToolBarContent>
        <HeaderContent>
            <MudTh><MudTableSortLabel SortBy="new Func<ClosedQuestion, object>(x=>x.Category.Name)">@L["Category"]</MudTableSortLabel></MudTh>
            <MudTh><MudTableSortLabel SortBy="new Func<ClosedQuestion, object>(x=>x.Name)">@L["ClosedQuestion"]</MudTableSortLabel></MudTh>
            <MudTh>@L["Actions"]</MudTh>
        </HeaderContent>
        <RowTemplate>
            <MudTd DataLabel="Id">@context.Id</MudTd>
            <MudTd DataLabel="Category">@context.Category.Name</MudTd>
            <MudTd DataLabel="ClosedQuestion">@context.Name</MudTd>
            <MudTd DataLabel="Actions">
                <MudButton OnClick="@(()=>QuestionDisplay(context))">@L["Display"]</MudButton>
                <MudButton OnClick="@(()=>QuestionDialog(context))">@L["Edit"]</MudButton>
            </MudTd>
        </RowTemplate>
        <PagerContent>
            <MudTablePager HidePagination="true" HorizontalAlignment="HorizontalAlignment.Right" Class="pl-2 pr-2"
                           PageSizeOptions="new int[] { 5, 10, 25, 50, 100 }" RowsPerPageString=@L["Rows per page"]
                           InfoFormat="{first_item}-{last_item} / {all_items}" />
        </PagerContent>
    </MudTable>

    <MudItem class="d-flex flex-wrap mt-4">
        <MudItem style="min-width:200px;">
            <MudText Inline="true" Class="align-self-center">@L["Selected1"]: @selectedItem1?.Name</MudText>
        </MudItem>
    </MudItem>
}
<style>
    .mud-tabs-panels {
        min-height: 200px !important;
    }
</style>