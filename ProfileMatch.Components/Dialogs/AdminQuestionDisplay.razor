<MudDialog>
    <DialogContent>
        <MudText Typo="Typo.h6">@Q.Category.Name: @Q.Name</MudText>
       

        <MudTabs Outlined="true"
                 Position="Position.Top"
                 Rounded="true"
                 Border="true"
                 ApplyEffectsToContainer="true"
                 PanelClass="pa-6 d-block">
            @foreach (var level in Q.AnswerOptions)
            {
                <MudTabPanel Text="@level.Level.ToString()">

                    <MudItem>
                        <MudText Typo="Typo.subtitle1">@L["Level"] @level.Level.ToString()</MudText>
                        <MudText Typo="Typo.subtitle2"
                                 @bind-Value="@level.Description">@level.Description</MudText>

                                <MudButton Variant="Variant.Filled" Color="Color.Tertiary" Size="Size.Small" OnClick="@(() => EditLevelDialog(level))">@L["Edit Level"]</MudButton>
                            </MudItem>
                        </MudTabPanel>
                    }
        </MudTabs>
        @if (CanActivate(Q))
        {
            <MudCheckBox @bind-Checked="Q.IsActive" @onclick="@(()=>IsActive())">@L["Activate question"]</MudCheckBox>
        }
    </DialogContent>
</MudDialog>

<style>
    .mud-tab {
        min-width: 50px !important;
        /*        border-bottom-style: none;*/
    }
    /*.mud-tab.mud-tab-active {
            border-bottom-style: solid;
            border-color: rgba(255,255,255, 0.70);
        }*/
</style>